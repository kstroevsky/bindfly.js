# Revision 4 semantic verification

## Delta pass

PASS. S13R-01 through S13R-07A are each mapped and implemented. Stage 13 is split into 13A Causality runtime, 13B Formula lenses, 13C Snapshot analysis, 13D Rips topology and 13E Analysis experience with the approved immediate dependency order. The candidate includes frozen hot updates, one exact-step protocol, one-pass comparison data, shared Probe scope, canonical tracing, derived point-cloud snapshots, a separate analysis Worker/scheduler, pinned analysis ε, safe Rips enumeration, validated β₁, cached persistence, presentation-state migration, perturbation history, controlled-phase semantics, declared formula parameters, separate sweep modes and deferred sensitivity.

## Preservation pass

PASS. All revision-3 requirements remain present: the user-visible Stage 13 question, Difference/Probe/Freeze/structure lenses, filled triangles, validated persistent-homology ordering, Explore/Compare/Analyze, exact historical Original labeling, later controlled/parameterized/sweep work, current visual language, analysis/derivation separation, visible failure/provenance and the Stage 14–17 order. Unchanged sections remain byte-identical to the locked base.

## Exact-contract pass

PASS. The following exact contracts are retained structurally:

- Run/Freeze/Step and one fixed-step main/Worker parity sequence;
- comparison arrays `ax/ay/bx/by/mx/my/dx/dy/magnitude/validity`;
- comparison `count`, an ADR-frozen side-validity encoding and ADR-frozen invalid-slot missingness consumed only after checking `validity`;
- \(dx\), \(dy\), \(\Delta\), 95th-percentile normalization and invalid-side handling;
- shared formula scope, trace-site instruction/node/source identity and source-reconstructed debug metadata;
- PointCloud source enum, Euclidean metric, CSS-pixel units and typed arrays;
- analysis result identity/provenance and separate Worker ownership;
- \({500\choose3}=20,708,500\), adjacency intersections and illustrative-only 50k/500k bands;
- graph cycle-rank and \(\beta_1\) equations plus the seven named validation fixtures;
- edge/triangle filtration births and one persistence computation per snapshot;
- \(a_i=a_{controlled}\), formula-parameter declaration/collision rules and the two distinct sweep modes;
- deferred finite-difference sensitivity formula plus reuse of frozen state, declared formula parameters and the comparison buffer.

No illustrative triangle threshold is promoted to an approved default and no ordering is imposed between the two Stage 17 sweep modes beyond keeping them distinct.

## Negative-control pass

PASS. The candidate preserves exact Original simulation semantics without introducing a path-specific implementation prohibition. It does not interpolate AST text, stream full buffers for Probe, create an arbitrary RPC framework, couple analysis ε to visual radius, run expensive analysis in the render Worker, use a cubic triangle loop, label graph cycles β₁, recompute persistence for every ε change, merge frozen and dynamic sweep semantics, start collaborative formula editing in the first multiplayer slice, or bring sensitivity into Stage 13.

## Cross-interface pass

PASS.

- UI/Runtime command → fixed-step loop → session: frozen hot events apply and render without simulation evolution; Step advances exactly one clock step on main and Worker.
- Step refreshes live derived/unpinned analysis provenance while an explicitly pinned PointCloud snapshot remains immutable until Analyze this frame is requested again.
- Formula compiler/evaluator → comparison buffer → Morph/A/B/Difference renderers: A/B executes once and every view consumes one result.
- Simulation/viewport/parameters → shared scope builder → derivation and Probe: renderer and inspector cannot calculate different inputs.
- Source AST/IR → compiler debug table → optional VM trace sink: trace semantics and import trust remain inside the canonical evaluator.
- Worker selection request → session inspection → small Probe payload: no per-frame state mirroring.
- Simulation snapshot → PointCloudSnapshot → separate analysis Worker/scheduler → active-request-only UI: topology cannot read live mutable state or block rendering.
- Pinned PointCloud plus analysis ε → structure/Rips/persistence → Analyze workspace: scale changes preserve coordinates, metric and provenance.
- Running `connectionRadius` → live visual derivation remains distinct from pinned analysis ε across the Flying Lines bridge, Rips definition, migration phase and Structure panel.
- ExperimentConfiguration versus StudioConfiguration → state codec migration → URLs/import: Formula Morph remains mathematical while workspace/lens/formulaView becomes presentation.
- Declared formula parameters → Controlled mode → frozen/dynamic sweeps: implementation and provenance precede automation.

## Standalone pass

PASS. The candidate supplies executable ordering, interfaces, enum members, formulas, validation fixtures, failure behavior, worker ownership, migration requirements and deferrals. It does not rely on conversational context or implicit prior-plan references for the amended contracts.

## Repair verification

The final repair removed three independently demonstrated losses/drifts: a path-specific Controlled-mode prohibition not authorized by the delta, an unapproved sequencing word between the two sweep modes, and omission of declared formula parameters from sensitivity reuse. The candidate was rebuilt from the locked base plus the original delta and these bounded repairs. Mechanical verification was rerun and a fresh independent verifier then returned PASS with zero unauthorized changes, dangling dependencies, cross-interface contradictions or standalone gaps.

## Disposition

Semantic verification: PASS. Revision 4 is ready for promotion with independent verification PASS.
